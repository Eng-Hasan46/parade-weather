import { useMemo, useState } from "react";import MapPicker from "./components/MapPicker.jsx";import SearchForm from "./components/SearchForm.jsx";import DualAxisChart from "./components/DualAxisChart.jsx";import { getForecast } from "./lib/weather.js";import { heatIndexC, verdict, fmt, labels as LBL } from "./lib/utils.js";import "./index.css";
export default function App(){const[lang,setLang]=useState('en');const L=LBL[lang];const[place,setPlace]=useState(null);const[date,setDate]=useState(new Date().toISOString().slice(0,10));const[time,setTime]=useState("Morning (6AM–12PM)");const[data,setData]=useState(null);const[sum,setSum]=useState(null);const[loading,setLoading]=useState(false);
async function onPick(p){setPlace(p);setLoading(true);try{const f=await getForecast(p.lat,p.lon);setData(f);const hrs=f.hourly;const idx=hrs.time.reduce((a,t,i)=>{if(t.startsWith(date))a.push(i);return a;},[]);const target=idx.find(i=>hrs.time[i].endsWith("12:00"))??(idx.length?idx[Math.floor(idx.length/2)]:null);if(target!=null){const pop=hrs.precipitation_probability[target]??0;const uv=hrs.uv_index[target]??0;const temp=hrs.temperature_2m[target]??0;const app=hrs.apparent_temperature[target]??temp;const wind=hrs.wind_speed_10m[target]??0;setSum(verdict({pop,uv,apparentC:heatIndexC(temp,60),wind}));}}finally{setLoading(false);}}
const snapshot=useMemo(()=>{if(!data)return null;const hrs=data.hourly;const idx=hrs.time.reduce((a,t,i)=>{if(t.startsWith(date))a.push(i);return a;},[]);const t=idx.find(i=>hrs.time[i].endsWith("12:00"))??(idx.length?idx[Math.floor(idx.length/2)]:null);if(t==null)return null;return{temp:hrs.temperature_2m[t],pop:hrs.precipitation_probability[t],uv:hrs.uv_index[t],wind:hrs.wind_speed_10m[t]};},[data,date]);
return(<div className='max-w-6xl mx-auto p-6 text-white'><div className='flex items-center justify-between mb-4'><div className='text-lg font-bold'>☔️ ParadeWeather</div><div className='flex items-center gap-2'><span className='badge'>🚀 NASA Space Apps 2025</span><button onClick={()=>setLang(l=>l==='en'?'ar':'en')} className='rounded-full px-3 py-1 bg-white/10 hover:bg-white/20'>{lang==='en'?'AR':'EN'}</button></div></div><section className='card p-8 mb-6 text-center'><h1 className='text-4xl font-extrabold bg-gradient-to-r from-ocean-500 to-leaf-500 inline-block text-transparent bg-clip-text drop-shadow'>{lang==='ar'?'لا تدع المطر يفسد خططك!':"Don't Let Rain Ruin Your Plans!"}</h1><p className='mt-3 text-white/80'>{L.subtitle}</p></section><SearchForm lang={lang} labels={L} onPick={onPick} date={date} setDate={setDate} time={time} setTime={setTime}/><div className='my-6'><MapPicker point={place} onPick={onPick}/></div>{place&&(<div className='grid md:grid-cols-3 gap-4 mb-6'><div className='md:col-span-1 card p-5'><div className='text-white/80 text-sm'>{place.name}</div><div className='text-3xl mt-2'>{sum?`${sum.icon} ${lang==='ar'?sum.ar:sum.en}`:(loading?'…':'')}</div></div><div className='md:col-span-2 card p-5'><div className='grid grid-cols-2 md:grid-cols-4 gap-4'><div className='text-center'><div className='text-white/70 text-sm'>{L.temp}</div><div className='text-2xl font-semibold'>{snapshot?fmt(snapshot.temp,'°C'):'--'}</div></div><div className='text-center'><div className='text-white/70 text-sm'>{L.precip}</div><div className='text-2xl font-semibold'>{snapshot?fmt(snapshot.pop,'%'):'--'}</div></div><div className='text-center'><div className='text-white/70 text-sm'>{L.uv}</div><div className='text-2xl font-semibold'>{snapshot?Math.round(snapshot.uv):'--'}</div></div><div className='text-center'><div className='text-white/70 text-sm'>{L.wind}</div><div className='text-2xl font-semibold'>{snapshot?fmt(snapshot.wind,' km/h'):'--'}</div></div></div></div></div>)}{place&&data&&<DualAxisChart data={data} date={date} labels={L}/>}<footer className='text-center text-white/60 text-xs mt-8'>Data: Open-Meteo • Prototype only — not for safety-critical use</footer></div>);}
